import { ValidacionTelefonoDirective } from './validacion-telefono.directive';

describe('ValidacionTelefonoDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionTelefonoDirective();
    expect(directive).toBeTruthy();
  });
});
