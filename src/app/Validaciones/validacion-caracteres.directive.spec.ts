import { ValidacionCaracteresDirective } from './validacion-caracteres.directive';

describe('ValidacionCaracteresDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionCaracteresDirective();
    expect(directive).toBeTruthy();
  });
});
